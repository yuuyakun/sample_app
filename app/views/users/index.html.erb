<%= provide(:title, "All users") %>

<h1>All users</h1>

<%= will_paginate %> <!-- ページ割 -->


<ul class="users">
    <% @users.each do |user| %>
      <%= render user %>  <!-- リファクタリング-->
    <% end %>
</ul>

<%= will_paginate %>  <!-- ページ割 -->
